<nav role="navigation" aria-label="pagination" class="{{ this.paginationClasses }}" {{ attributes }}>
    <ul class="flex flex-row items-center gap-1">
        {% block content %}{% endblock %}
    </ul>
</nav>
